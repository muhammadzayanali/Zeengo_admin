/**
 * Zeengo Admin (Command Center) — source layout
 *
 * src/
 *   app/                 App shell, router, guards, navigation
 *   modules/
 *     auth/              Staff login, permissions, RBAC helpers
 *     admin/             Admin-only: users, settings, roles stubs
 *     command/           Ops control: dashboard, ops room, daily ops, SOS, tasks
 *     clients/           CRM: clients, bookings, edit-requests, VIP
 *       components/      New client form, detail panels (driver/task/edit)
 *       pages/           List, new, and client detail
 *       bookings|vip|edit-requests/  Sub-modules
 *     fleet/             Drivers, vendors
 *     finance/           Finance, payments, packages, splizer
 *     tools/             Chat, notifications, AI, email
 *   shared/              UI, API client, i18n, hooks
 *   ops-demo/            Demo data store (until fully API-backed)
 */
export {};
